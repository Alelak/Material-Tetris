// Définit une poistion x,y
function Point(x, y) {
    this.x = x;
    this.y = y;
    return this;
}

// Définit une largeur hauteur
function Dim(w, h) {
    this.w = w;
    this.h = h;
    return this;
}